# MB0 — method-native backend baseline

MB0 starts from annotated tag `v5-final-pre-calibration-no-go-v1` at commit
`d0c34fa98859e0c14d4c253595122f48e32b18e8` on branch
`feature/v5-final-method-native-backends-v1`.

The parent evidence repository is pinned at `4783b9ff9f9b6f2061a1ef8c02613f4c6cef38db`.
Its CEO* implementation submodule is pinned at
`a3f89d03e6a03c89767d3cf8ee7657a57653dda0`. Root and parent dependency locks
are content-addressed in the MB0 artifact.

All artifacts tracked by the baseline tag are inventoried by SHA-256. The
development queue remains 90/90 `NOT_STARTED`, with zero completed items, zero
segments, and zero development candidate-energy evaluations. An empty ledger
is explicitly incomplete.

A new recursive clone of the public GitHub repository reproduced `85 passed,
3 xfailed`. The first temporary clone hit local disk exhaustion. Two clean,
ignored, reproducible historical audit clones were removed; a newly created
recursive clone then completed and reproduced the baseline.

MB0 authorizes code-level parent implementation research only. It does not
authorize H2/H4 candidate energy, the 90-item development queue, or any
performance claim.
